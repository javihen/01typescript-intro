let name:string = 'strider';

/* Si colocamos el simbolo | podemos usar tanto dos tipos de datos */
let hpPoints: number | string = 95;
hpPoints = 'fell';

const isAliev: boolean = true;

console.log({name, hpPoints, isAliev })

export { };